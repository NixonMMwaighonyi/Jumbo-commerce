# Use a lightweight web server
FROM nginx:alpine

# Copy your index.html into the server's public folder
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 (the default web port)
EXPOSE 80